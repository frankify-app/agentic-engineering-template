## Preference set

<!-- d10e: auto-prune -->
<!-- Copier-vendored from the agentic-engineering-template — do NOT edit
     here; change it in the template and pull via `copier update`. The
     auto-prune marker above lets `disambiguate prune` remove this term
     from a repo that never links it. -->

The active preference rules (`preferences.md` in the decision store) — the
only file injected into agent sessions, kept under a hard token budget.
Confirmation counters on each rule are the one sanctioned edit; promotion of
new rules is human-only.
